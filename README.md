# Restricted Pages Access

Плагин для WordPress, который позволяет ограничивать доступ пользователей с ролью **Editor** (Редактор) к конкретным страницам и записям в административной панели.

## 📋 Описание

**Проблема:** По умолчанию пользователи с ролью Editor видят ВСЕ страницы и записи на сайте.

**Решение:** Этот плагин позволяет администратору точечно настроить, какие именно страницы и записи видит каждый редактор в админ-панели. Все остальные страницы и записи будут скрыты.

## ✨ Возможности

- ✅ Ограничение доступа редакторов к конкретным страницам
- ✅ Ограничение доступа редакторов к конкретным записям (posts)
- ✅ Блокировка прямого доступа по URL к запрещенным страницам (403 Forbidden)
- ✅ Фильтрация выпадающих списков (родительские страницы)
- ✅ Журнал попыток несанкционированного доступа
- ✅ Удобный административный интерфейс
- ✅ Безопасность (nonce, валидация, санитизация)
- ✅ Полностью протестирован (unit-тесты)

## 📦 Установка

### Способ 1: Установка через WordPress Admin (рекомендуется)

1. **Скачайте плагин** или создайте ZIP-архив:
   ```bash
   cd /путь/к/wp-user-roles-restrict
   zip -r restricted-pages-access.zip . -x "*.git*" "idea.md" "test-restricted-pages-access.php"
   ```

2. **Загрузите в WordPress:**
   - Зайдите в админ-панель WordPress
   - Перейдите: `Плагины → Добавить новый → Загрузить плагин`
   - Выберите файл `restricted-pages-access.zip`
   - Нажмите `Установить`

3. **Активируйте плагин:**
   - После установки нажмите `Активировать`

### Способ 2: Установка вручную (через FTP/SSH)

1. **Скопируйте папку плагина** на сервер:
   ```bash
   # Если используете SSH/SFTP
   scp -r wp-user-roles-restrict user@yourserver.com:/var/www/html/wp-content/plugins/
   ```

2. **Или загрузите через FTP:**
   - Подключитесь к серверу через FTP-клиент (FileZilla, Cyberduck)
   - Перейдите в папку `wp-content/plugins/`
   - Загрузите папку `wp-user-roles-restrict` целиком

3. **Переименуйте папку** (если нужно):
   ```bash
   mv wp-user-roles-restrict restricted-pages-access
   ```

4. **Активируйте плагин:**
   - Зайдите в админ-панель WordPress
   - Перейдите: `Плагины`
   - Найдите **Restricted Pages Access** и нажмите `Активировать`

### Способ 3: Установка через WP-CLI

```bash
# Перейдите в корень WordPress
cd /var/www/html/wordpress

# Скопируйте плагин в директорию плагинов
cp -r /путь/к/wp-user-roles-restrict wp-content/plugins/restricted-pages-access

# Активируйте плагин
wp plugin activate restricted-pages-access
```

## 🚀 Быстрый старт

### 1. Откройте настройки плагина

После активации перейдите:
```
Админ-панель → Параметры → Ограничение доступа
```

### 2. Настройте доступ для редактора

1. **Выберите пользователя** из списка редакторов
2. Нажмите кнопку **"Редактировать доступ"**
3. **Отметьте галочками** разрешенные страницы и записи:
   - В блоке "Страницы" — выберите нужные страницы
   - В блоке "Записи" — выберите нужные записи
4. Нажмите **"Сохранить изменения"**

### 3. Проверьте результат

1. Выйдите из админ-панели
2. Войдите под учетной записью редактора
3. Откройте `Страницы` или `Записи`
4. **Результат:** Редактор видит только разрешенные элементы!

## 📖 Использование

### Пример 1: Дать доступ верстальщику только к двум страницам

**Сценарий:** У вас есть верстальщик Иван с ролью Editor. Вы хотите дать ему доступ только к страницам "О компании" и "Услуги".

**Шаги:**
1. Перейдите: `Параметры → Ограничение доступа`
2. Найдите пользователя "Иван" в списке
3. Нажмите "Редактировать доступ"
4. В блоке "Страницы" отметьте:
   - ☑ О компании
   - ☑ Услуги
5. Нажмите "Сохранить изменения"

**Результат:**
- Иван видит в админке только 2 страницы
- Все остальные страницы скрыты
- При попытке открыть другую страницу по прямой ссылке — получит ошибку 403

### Пример 2: Дать доступ к нескольким записям

**Сценарий:** Контент-менеджер должен редактировать только записи в категории "Новости".

**Шаги:**
1. Откройте список записей и запомните ID нужных постов
2. Перейдите: `Параметры → Ограничение доступа`
3. Выберите контент-менеджера
4. В блоке "Записи" отметьте нужные посты
5. Сохраните

### Журнал доступа

Плагин автоматически записывает все попытки несанкционированного доступа:

1. Перейдите: `Параметры → Ограничение доступа`
2. Откройте вкладку **"Журнал доступа"**
3. Просмотрите таблицу с данными:
   - Время попытки
   - Пользователь
   - К какой странице пытался получить доступ
   - IP-адрес

**Очистка журнала:**
- Нажмите кнопку "Очистить журнал" внизу страницы

## 🏗️ Структура плагина

```
restricted-pages-access/
├── restricted-pages-access.php       # Главный файл плагина
├── includes/                         # Классы плагина
│   ├── class-admin-page.php          # Административный интерфейс
│   ├── class-access-filter.php       # Фильтрация списков в админке
│   ├── class-post-access.php         # Проверка доступа к отдельным постам
│   └── class-user-meta-handler.php   # Работа с метаданными пользователей
├── test-restricted-pages-access.php  # Unit-тесты
├── idea.md                           # Техническое задание
├── README.md                         # Документация (этот файл)
└── readme.txt                        # Описание для WordPress.org
```

## 🔧 Технические требования

- **WordPress:** 5.8 или выше
- **PHP:** 7.4 или выше
- **MySQL:** 5.6 или выше

## 🧪 Тестирование

Плагин включает полный набор unit-тестов:

```bash
# Установите WordPress Test Suite
bash bin/install-wp-tests.sh wordpress_test root '' localhost latest

# Запустите тесты
phpunit test-restricted-pages-access.php
```

**Покрытие тестами:**
- ✅ Сохранение и получение метаданных
- ✅ Фильтрация списков для редакторов
- ✅ Фильтрация выпадающих списков
- ✅ Администратор видит всё
- ✅ Блокировка прямого доступа
- ✅ Разрешение доступа к разрешенным постам
- ✅ Логирование попыток доступа
- ✅ Очистка журнала

## 🔐 Безопасность

Плагин соблюдает все стандарты безопасности WordPress:

- ✅ **Nonce проверки** для всех POST-запросов
- ✅ **Проверка прав** `current_user_can('manage_options')`
- ✅ **Валидация данных** перед сохранением в БД
- ✅ **Санитизация** всех входных данных (`sanitize_text_field`, `intval`)
- ✅ **Escapement** всех выводимых данных (`esc_html`, `esc_url`, `esc_attr`)
- ✅ **Защита от прямого доступа** к PHP-файлам

## 🛠️ Хранение данных

Плагин хранит данные о правах доступа в таблице `wp_usermeta`:

```
user_id: 5 (Editor)
├── meta_key: 'rpa_allowed_pages'
│   └── meta_value: [12, 45, 67]  // Массив ID разрешенных страниц
└── meta_key: 'rpa_allowed_posts'
    └── meta_value: [89, 102]     // Массив ID разрешенных записей
```

Журнал доступа хранится в `wp_options`:
```
option_name: 'rpa_access_logs'
option_value: [
  {
    'time': '2025-12-15 10:30:45',
    'user_login': 'ivan',
    'post_id': 123,
    'post_title': 'Запрещенная страница',
    'ip': '192.168.1.100'
  },
  ...
]
```

## ❓ FAQ

### Как работает ограничение доступа?

Плагин использует три механизма:
1. **Фильтр `pre_get_posts`** — скрывает недоступные посты из списков в админке
2. **Хук `load-post.php`** — блокирует прямой доступ к редактору поста
3. **Фильтр `page_attributes_dropdown_pages_args`** — скрывает недоступные страницы из выпадающих списков

### Влияет ли плагин на администраторов?

Нет. Администраторы (с правом `manage_options`) видят все страницы и записи без ограничений.

### Можно ли использовать для Custom Post Types?

В текущей версии (1.0.0) поддерживаются только стандартные Pages и Posts. Поддержка Custom Post Types планируется в версии 1.1.

### Работает ли плагин с Elementor?

Да, плагин совместим с Elementor и другими page builders.

### Как удалить все данные плагина?

При деактивации плагин НЕ удаляет данные автоматически. Для полной очистки:

```php
// Вручную удалите метаданные пользователей
delete_metadata('user', 0, 'rpa_allowed_pages', '', true);
delete_metadata('user', 0, 'rpa_allowed_posts', '', true);

// Удалите журнал доступа
delete_option('rpa_access_logs');
```

### Что произойдет, если удалить страницу, к которой дан доступ?

Плагин не проверяет валидность ID при каждом запросе, поэтому ID удаленной страницы останется в метаданных. Это не вызовет ошибок, но при редактировании прав в админке можно очистить список.

## 🐛 Известные ограничения

1. **Производительность на больших сайтах:** При редактировании прав загружаются ВСЕ страницы и записи. На сайтах с 1000+ постов это может быть медленно. Рекомендуется добавить AJAX-поиск.

2. **Отсутствие кэширования:** Права доступа читаются из БД при каждом запросе. Можно добавить кэширование через транзиенты.

3. **Нет локализации:** Все тексты на русском языке, не используются функции `__()` для перевода.

4. **REST API:** Фильтры для REST API не реализованы в версии 1.0.

## 🗺️ Дорожная карта

### Версия 1.1 (планируется)
- [ ] Локализация (русский/английский)
- [ ] REST API фильтры
- [ ] AJAX-поиск в интерфейсе выбора страниц
- [ ] Кэширование через транзиенты

### Версия 1.2 (планируется)
- [ ] Поддержка Custom Post Types
- [ ] Групповое управление (назначение прав группе пользователей)
- [ ] Экспорт/импорт настроек

### Версия 2.0 (планируется)
- [ ] Интеграция с Elementor (проверка доступа в Elementor Editor)
- [ ] Ограничение на уровне таксономий (категории, метки)
- [ ] Временные ограничения (дать доступ на период)

## 👨‍💻 Разработка

### Требования для разработки
```bash
# Установите зависимости
composer install

# Настройте окружение для тестов
bash bin/install-wp-tests.sh wordpress_test root '' localhost latest

# Запустите тесты
phpunit
```

### Стандарты кодирования

Плагин следует [WordPress Coding Standards](https://developer.wordpress.org/coding-standards/wordpress-coding-standards/):
- Tab-индентация
- Yoda conditions
- Проверки безопасности

## 📄 Лицензия

GPL v2 или более поздняя

## 🤝 Вклад в проект

Баг-репорты и pull requests приветствуются!

## 📞 Поддержка

При возникновении проблем:
1. Проверьте раздел FAQ выше
2. Убедитесь, что версии WordPress и PHP соответствуют требованиям
3. Включите режим отладки WordPress (`WP_DEBUG`)
4. Проверьте логи сервера

## 📜 История изменений

### 1.0.0 (2025-12-15)
- ✨ Первый релиз
- ✅ Базовая функциональность (MVP)
- ✅ Ограничение доступа к страницам и записям
- ✅ Административный интерфейс
- ✅ Журнал доступа
- ✅ Unit-тесты

---

**Автор:** Gemini Code Assist
**Версия:** 1.0.0
**Требует WordPress:** 5.8+
**Протестировано до:** 6.4
**Лицензия:** GPLv2
